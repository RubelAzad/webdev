<?php

namespace Modules\Cargo\Database\Seeders;

use Illuminate\Database\Seeder;
use Illuminate\Database\Eloquent\Model;
use App\Ability;

class CargoAbilityTableSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
        Model::unguard();

//        $m = 'Shipment';
//        Ability::where('model', $m)->delete();
//        Ability::firstOrCreate(['title' => 'Create New Shipment', 'ability' => 'create_shipment', 'model' => $m]); // move this line to Shipment module

        // moved to Shipment module
        // moved to Shipment module
        // moved to Shipment module
        // moved to Shipment module
    }
}
